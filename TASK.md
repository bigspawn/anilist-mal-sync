Нужно выяснить, проанализировать проблему в поиске данных в --reverse-direction режиме

Вот есть собранные логи `2.log`

Можешь добавлять любые логи дополнительные и запускать выполние ТОЛЬКО ТАК КАК ОПИСАНО НИЖЕ

Запускать для проверки можно ТОЛЬКО ТАК:
`go run . -c ~/.config/anilist-mal-sync/config.yaml sync --reverse-direction -d --verbose`

ТОЛЬКО В `--dry-run, -d               dry run without updating target service`
